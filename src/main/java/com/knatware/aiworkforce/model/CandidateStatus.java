package com.knatware.aiworkforce.model;

/** Stages in the recruitment pipeline. */
public enum CandidateStatus {
    APPLIED,
    SCREENING,
    AI_INTERVIEW,
    SHORTLISTED,
    OFFER,
    HIRED,
    REJECTED
}
